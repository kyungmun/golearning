package main

/*
func updateProgress2(jobName string, progress int) {
	fmt.Printf("\r%s: %s>%s (%d%%)", jobName, strings.Repeat("=", progress), strings.Repeat(" ", 100-progress), progress)
}

func main2() {
	for i := 0; i <= 100; i++ {
		updateProgress("job1", i)
		time.Sleep(100 * time.Millisecond) // 진행 상태를 확인하기 위한 간단한 딜레이, 실제 작업 중에는 이 부분 대체 필요
	}

	fmt.Println() // 진행 상태 표시를 마치고 줄 바꿈
}
*/
